<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/walking1"
    tools:context=".Steps" >
    
    <TextView 
        android:id="@+id/walktxtListHeading"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Paired devices appear below"
        android:textSize="20sp"
        android:gravity="center"
        android:textColor="#00ff00"
        android:textStyle="bold"
        />

    <ListView
        android:id="@+id/walklstDevices"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingBottom="10dp" >

    </ListView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/walkbtnSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="2"
             android:background="@drawable/custom_newmenu1"
            android:text="Search for paired devices" 
            android:textColor="#FFFFFF"
            android:alpha="0.7"
         />

        <Button
            android:id="@+id/walkbtnConnect"
            android:layout_width="0dp"
            android:layout_height="fill_parent"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:enabled="false"
            android:background="@drawable/custom_newmenu1"
            android:text="Connect" 
            android:textColor="#FFFFFF"
            android:alpha="0.7"/>

    </LinearLayout>
</LinearLayout>


